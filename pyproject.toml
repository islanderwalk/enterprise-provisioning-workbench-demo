[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-provisioning-workbench-demo"
version = "0.2.0"
description = "Sanitized demonstration of a deterministic enterprise provisioning workflow"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.setuptools]
packages = ["demo_app"]
